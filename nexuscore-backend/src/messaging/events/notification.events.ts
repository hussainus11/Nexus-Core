export enum NotificationEvent {
  SEND = 'nexuscore.notification.send',
  HIGH_WASTAGE = 'nexuscore.notification.high_wastage',
  LOW_FABRIC_STOCK = 'nexuscore.notification.low_fabric_stock',
  BATCH_OVERDUE = 'nexuscore.notification.batch_overdue',
  DEFECT_THRESHOLD = 'nexuscore.notification.defect_threshold',
  ORDER_APPROVED = 'nexuscore.notification.order_approved',
  ORDER_REJECTED = 'nexuscore.notification.order_rejected',
  APPROVAL_NEEDED = 'nexuscore.notification.approval_needed',
}
